{
  "name": "video-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Screen-recording QA for Claude in Chrome: records a browser window and returns budgeted visual evidence the model can actually read.",
  "main": "src/server.mjs",
  "license": "ISC",
  "scripts": {
    "build:native": "node scripts/build-native.mjs",
    "doctor": "node scripts/doctor-cli.mjs",
    "test": "node --test test/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ffmpeg-static": "^5.3.0"
  }
}
